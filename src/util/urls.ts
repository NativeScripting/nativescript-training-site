export function trainingUrlFromId(trainingId: string): string {
    return `/training/${trainingId}`;
}


export function courseUrlFromId(courseId: string): string {
    return `/course/${courseId}`;
}

export function sessionUrlFrom(sessionId: string): string {
    return `/session/${sessionId}`;
}
